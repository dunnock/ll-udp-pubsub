[build]

# we need to disable AVX512 otherwise it won't run on some AWS instances
rustflags = [
	"-C", "target-cpu=cascadelake",
	"-C", "target-feature=-avx512cd,-avx512bw,-avx512vl,-avx512f,-avx512dq",
        "-C", "opt-level=3"
]
